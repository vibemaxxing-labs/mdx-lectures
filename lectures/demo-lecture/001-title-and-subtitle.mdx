---
title: Markdown Demo Slides
subtitle: MDX / React / Local files
---

## A quiet canvas for focused MDX slides

The runtime keeps the interface invisible so the content can own the full viewport.

<div className="metric-row">
  <Metric value="100vw" label="Viewport width" />
  <Metric value="100dvh" label="Viewport height" />
  <Metric value="0" label="Visible controls" />
</div>
