---
title: <i><b>Core</b></i> компоненти системи
subtitle: "*** ** *"
---

### з чого складається типова web-app система?

```mermaid
architecture-beta
  service user(internet)[User]
  service frontend(server)[Frontend]
  service backend(server)[Backend]
  service database(database)[Database]

  user:R <--> L:frontend
  frontend:R <--> L:backend
  backend:R <--> L:database
```
